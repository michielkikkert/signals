import { FormSignalsDirective } from './form-signals.directive';

describe('FormSignalsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSignalsDirective();
    expect(directive).toBeTruthy();
  });
});
